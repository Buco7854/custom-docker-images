# Build context is ./nginx but the Dockerfile only bakes parts of
# image/. Keep the example stack, docs, and the opt-in extras (never
# baked — copied by hand into a deployment) out of the context.
example/
README.md
.dockerignore
image/conf/optional/
