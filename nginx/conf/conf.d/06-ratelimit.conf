# Rate-limit zones. Per-server blocks opt in via snippets/ratelimit.conf.
# Connection limits use the conn_limit zone defined at http level in
# nginx.conf.

limit_req_zone $binary_remote_addr zone=req_limit:10m rate=20r/s;
limit_req_status 429;
limit_conn_status 429;
