# Docker-only: the CrowdSec bouncer's lua HTTP client needs a resolver
# to look up the `crowdsec` service name over the compose network
# (docker embedded DNS at 127.0.0.11). On a baremetal install talking to
# LAPI at 127.0.0.1 this file is unnecessary.
resolver 127.0.0.11 ipv6=off;
