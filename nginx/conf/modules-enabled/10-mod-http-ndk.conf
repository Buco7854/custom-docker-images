# Debian's libnginx-mod-http-ndk equivalent. Absolute path because this
# base ships nginx.org's nginx (prefix /etc/nginx), not Debian's, so a
# relative `modules/...` would not resolve. NDK must load before lua.
load_module /usr/lib/nginx/modules/ndk_http_module.so;
