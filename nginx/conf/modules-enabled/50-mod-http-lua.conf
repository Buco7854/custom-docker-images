# lua-nginx-module + LuaJIT, compiled into this image against the exact
# nginx uozi ships (absolute path; see 10-mod-http-ndk.conf for why and
# for the Debian-vs-nginx.org version note). Required by the CrowdSec
# bouncer.
load_module /usr/lib/nginx/modules/ngx_http_lua_module.so;
